# 3.2.4 Database Schema

The database schema describes the tables that support the current LYDO Connect workflow. To make the chapter easier to read, the ERD is presented in one overall figure and then broken into smaller subpart figures grouped by workflow. The schema is centered on authentication, policy acceptance, user profiles, barangay lookup data, organization profiles, required document types, document submission, budget requests, liquidation reporting, news releases, transparency posts, notifications, activity logs, and compliance remarks. Budget allocation monitoring is derived from the `budget_requests` and `organization_profiles` tables, especially the `released_amount`, `district`, and `barangay` fields.

## Overall Database Schema

```mermaid
erDiagram
    roles {
        smallint id PK
        app_role_code code UK
        text label
        text description
        timestamptz created_at
    }

    user_roles {
        uuid user_id PK, FK
        smallint role_id PK, FK
        timestamptz assigned_at
        uuid assigned_by FK
    }

    user_profiles {
        uuid user_id PK, FK
        citext email UK
        text full_name
        text display_name
        text contact_number
        text municipality
        text barangay
        text avatar_url
        timestamptz created_at
        timestamptz updated_at
    }

    admin_accounts {
        uuid id PK
        citext username UK
        text password_hash
        citext email UK
        text display_name
        boolean is_active
        timestamptz created_at
        timestamptz updated_at
    }

    admin_sessions {
        uuid id PK
        uuid admin_id FK
        text token_hash UK
        timestamptz expires_at
        timestamptz last_used_at
        timestamptz revoked_at
        timestamptz created_at
    }

    policy_versions {
        uuid id PK
        text version UK
        text title
        text terms_content
        text privacy_content
        boolean is_active
        date effective_date
        timestamptz created_at
        timestamptz updated_at
    }

    user_policy_acceptance {
        uuid id PK
        uuid user_id FK
        uuid policy_version_id FK
        boolean accepted_terms
        boolean accepted_privacy
        timestamptz accepted_at
        text accepted_ip
        text user_agent
        timestamptz created_at
    }

    barangays {
        uuid id PK
        text name UK
        timestamptz created_at
        timestamptz updated_at
    }

    organization_profiles {
        uuid id PK
        uuid user_id UK, FK
        text organization_name
        citext organization_email
        text contact_number
        text district
        text barangay
        text major_classification
        text sub_classification
        text[] advocacies
        text adviser_name
        text representative_name
        text address
        text facebook_page_url
        profile_status profile_status
        timestamptz verified_at
        text internal_notes
        timestamptz created_at
        timestamptz updated_at
    }

    required_document_types {
        uuid id PK
        text name UK
        text description
        integer required_year
        boolean is_required
        text template_url
        text template_description
        integer sort_order
        boolean is_active
        timestamptz created_at
        timestamptz updated_at
    }

    document_submissions {
        uuid id PK
        uuid organization_id FK
        uuid submitted_by FK
        document_submission_status status
        boolean user_confirmed
        timestamptz submitted_at
        uuid reviewed_by FK
        timestamptz reviewed_at
        text overall_remarks
        timestamptz created_at
        timestamptz updated_at
    }

    document_submission_files {
        uuid id PK
        uuid submission_id FK
        uuid document_type_id FK
        text file_url
        text file_name
        text file_type
        bigint file_size
        text ocr_text
        ocr_status ocr_status
        numeric ocr_confidence
        document_file_validation_status validation_status
        document_submission_status admin_status
        text admin_remarks
        jsonb ocr_metadata
        timestamptz uploaded_at
        timestamptz reviewed_at
        timestamptz created_at
        timestamptz updated_at
    }

    budget_requests {
        uuid id PK
        uuid organization_id FK
        uuid submitted_by FK
        text activity_title
        text activity_description
        date activity_date
        text venue
        numeric requested_amount
        numeric approved_amount
        numeric released_amount
        date release_date
        text purpose_category
        budget_request_status status
        text remarks
        timestamptz go_signal_at
        timestamptz hard_copy_submitted_at
        timestamptz created_at
        timestamptz updated_at
    }

    budget_request_files {
        uuid id PK
        uuid budget_request_id FK
        text file_url
        text file_name
        text file_type
        bigint file_size
        timestamptz uploaded_at
        timestamptz created_at
    }

    liquidation_reports {
        uuid id PK
        uuid budget_request_id UK, FK
        uuid organization_id FK
        uuid submitted_by FK
        liquidation_report_status status
        text remarks
        timestamptz go_signal_at
        timestamptz deadline_at
        timestamptz hard_copy_submitted_at
        timestamptz completed_at
        timestamptz created_at
        timestamptz updated_at
    }

    liquidation_report_files {
        uuid id PK
        uuid liquidation_report_id FK
        text file_url
        text file_name
        text file_type
        bigint file_size
        timestamptz uploaded_at
        timestamptz created_at
    }

    news_releases {
        uuid id PK
        text title
        text description
        text facebook_post_url
        date date_posted
        visibility_status visibility_status
        uuid created_by FK
        timestamptz created_at
        timestamptz updated_at
    }

    transparency_posts {
        uuid id PK
        text title
        text description
        text category
        text attachment_url
        visibility_status visibility_status
        date post_date
        uuid created_by FK
        timestamptz created_at
        timestamptz updated_at
    }

    compliance_remarks {
        uuid id PK
        uuid organization_id FK
        text related_type
        uuid related_id
        text remark_type
        text consequence_type
        text message
        compliance_remark_status status
        uuid created_by FK
        uuid resolved_by FK
        timestamptz resolved_at
        timestamptz created_at
        timestamptz updated_at
    }

    notifications {
        uuid id PK
        uuid user_id FK
        uuid organization_id FK
        text title
        text message
        notification_type type
        text related_type
        uuid related_id
        boolean is_read
        timestamptz created_at
    }

    activity_logs {
        uuid id PK
        uuid actor_user_id FK
        uuid organization_id FK
        text action
        text related_type
        uuid related_id
        text description
        timestamptz created_at
    }

    auth.users ||--|| user_profiles : user_id
    auth.users ||--o{ user_roles : user_id
    roles ||--o{ user_roles : role_id
    auth.users ||--o{ user_policy_acceptance : user_id
    policy_versions ||--o{ user_policy_acceptance : policy_version_id
    auth.users ||--o{ organization_profiles : user_id
    organization_profiles ||--o{ document_submissions : organization_id
    document_submissions ||--o{ document_submission_files : submission_id
    required_document_types ||--o{ document_submission_files : document_type_id
    organization_profiles ||--o{ budget_requests : organization_id
    budget_requests ||--o{ budget_request_files : budget_request_id
    budget_requests ||--o| liquidation_reports : budget_request_id
    liquidation_reports ||--o{ liquidation_report_files : liquidation_report_id
    auth.users ||--o{ news_releases : created_by
    auth.users ||--o{ transparency_posts : created_by
    auth.users ||--o{ compliance_remarks : created_by
    auth.users ||--o{ notifications : user_id
    auth.users ||--o{ activity_logs : actor_user_id
```

## Subpart 1. Authentication, Users, and Roles

This subpart shows the tables that control access, profiles, and policy acceptance.

## Figure 17.2. Entity Relationship Diagram of Authentication, Users, and Roles

```mermaid
erDiagram
    roles {
        smallint id PK
        app_role_code code UK
        text label
        text description
        timestamptz created_at
    }

    user_roles {
        uuid user_id PK, FK
        smallint role_id PK, FK
        timestamptz assigned_at
        uuid assigned_by FK
    }

    user_profiles {
        uuid user_id PK, FK
        citext email UK
        text full_name
        text display_name
        text contact_number
        text municipality
        text barangay
        text avatar_url
        timestamptz created_at
        timestamptz updated_at
    }

    admin_accounts {
        uuid id PK
        citext username UK
        text password_hash
        citext email UK
        text display_name
        boolean is_active
        timestamptz created_at
        timestamptz updated_at
    }

    admin_sessions {
        uuid id PK
        uuid admin_id FK
        text token_hash UK
        timestamptz expires_at
        timestamptz last_used_at
        timestamptz revoked_at
        timestamptz created_at
    }

    policy_versions {
        uuid id PK
        text version UK
        text title
        text terms_content
        text privacy_content
        boolean is_active
        date effective_date
        timestamptz created_at
        timestamptz updated_at
    }

    user_policy_acceptance {
        uuid id PK
        uuid user_id FK
        uuid policy_version_id FK
        boolean accepted_terms
        boolean accepted_privacy
        timestamptz accepted_at
        text accepted_ip
        text user_agent
        timestamptz created_at
    }

    barangays {
        uuid id PK
        text name UK
        timestamptz created_at
        timestamptz updated_at
    }

    auth.users ||--|| user_profiles : user_id
    auth.users ||--o{ user_roles : user_id
    roles ||--o{ user_roles : role_id
    auth.users ||--o{ user_policy_acceptance : user_id
    policy_versions ||--o{ user_policy_acceptance : policy_version_id
    admin_accounts ||--o{ admin_sessions : admin_id
```

## Subpart 2. Organization and Document Workflow

This subpart shows the data flow from organization registration to document review.

## Figure 17.3. Entity Relationship Diagram of Organization and Document Workflow

```mermaid
erDiagram
    organization_profiles {
        uuid id PK
        uuid user_id UK, FK
        text organization_name
        citext organization_email
        text contact_number
        text district
        text barangay
        text major_classification
        text sub_classification
        text[] advocacies
        text adviser_name
        text representative_name
        text address
        text facebook_page_url
        profile_status profile_status
        timestamptz verified_at
        text internal_notes
        timestamptz created_at
        timestamptz updated_at
    }

    required_document_types {
        uuid id PK
        text name UK
        text description
        integer required_year
        boolean is_required
        text template_url
        text template_description
        integer sort_order
        boolean is_active
        timestamptz created_at
        timestamptz updated_at
    }

    document_submissions {
        uuid id PK
        uuid organization_id FK
        uuid submitted_by FK
        document_submission_status status
        boolean user_confirmed
        timestamptz submitted_at
        uuid reviewed_by FK
        timestamptz reviewed_at
        text overall_remarks
        timestamptz created_at
        timestamptz updated_at
    }

    document_submission_files {
        uuid id PK
        uuid submission_id FK
        uuid document_type_id FK
        text file_url
        text file_name
        text file_type
        bigint file_size
        text ocr_text
        ocr_status ocr_status
        numeric ocr_confidence
        document_file_validation_status validation_status
        document_submission_status admin_status
        text admin_remarks
        jsonb ocr_metadata
        timestamptz uploaded_at
        timestamptz reviewed_at
        timestamptz created_at
        timestamptz updated_at
    }

    auth.users ||--o| organization_profiles : user_id
    organization_profiles ||--o{ document_submissions : organization_id
    document_submissions ||--o{ document_submission_files : submission_id
    required_document_types ||--o{ document_submission_files : document_type_id
```

## Subpart 3. Budget and Liquidation Workflow

This subpart shows the request, release, and liquidation cycle used for budget monitoring.

## Figure 17.4. Entity Relationship Diagram of Budget and Liquidation Workflow

```mermaid
erDiagram
    budget_requests {
        uuid id PK
        uuid organization_id FK
        uuid submitted_by FK
        text activity_title
        text activity_description
        date activity_date
        text venue
        numeric requested_amount
        numeric approved_amount
        numeric released_amount
        date release_date
        text purpose_category
        budget_request_status status
        text remarks
        timestamptz go_signal_at
        timestamptz hard_copy_submitted_at
        timestamptz created_at
        timestamptz updated_at
    }

    budget_request_files {
        uuid id PK
        uuid budget_request_id FK
        text file_url
        text file_name
        text file_type
        bigint file_size
        timestamptz uploaded_at
        timestamptz created_at
    }

    liquidation_reports {
        uuid id PK
        uuid budget_request_id UK, FK
        uuid organization_id FK
        uuid submitted_by FK
        liquidation_report_status status
        text remarks
        timestamptz go_signal_at
        timestamptz deadline_at
        timestamptz hard_copy_submitted_at
        timestamptz completed_at
        timestamptz created_at
        timestamptz updated_at
    }

    liquidation_report_files {
        uuid id PK
        uuid liquidation_report_id FK
        text file_url
        text file_name
        text file_type
        bigint file_size
        timestamptz uploaded_at
        timestamptz created_at
    }

    organization_profiles ||--o{ budget_requests : organization_id
    budget_requests ||--o{ budget_request_files : budget_request_id
    budget_requests ||--o| liquidation_reports : budget_request_id
    liquidation_reports ||--o{ liquidation_report_files : liquidation_report_id
```

## Subpart 4. Admin Content, Notifications, and Audit Logs

This subpart groups the admin-managed content tables and the system tracking tables.

## Figure 17.5. Entity Relationship Diagram of Admin Content, Notifications, and Audit Logs

```mermaid
erDiagram
    news_releases {
        uuid id PK
        text title
        text description
        text facebook_post_url
        date date_posted
        visibility_status visibility_status
        uuid created_by FK
        timestamptz created_at
        timestamptz updated_at
    }

    transparency_posts {
        uuid id PK
        text title
        text description
        text category
        text attachment_url
        visibility_status visibility_status
        date post_date
        uuid created_by FK
        timestamptz created_at
        timestamptz updated_at
    }

    compliance_remarks {
        uuid id PK
        uuid organization_id FK
        text related_type
        uuid related_id
        text remark_type
        text consequence_type
        text message
        compliance_remark_status status
        uuid created_by FK
        uuid resolved_by FK
        timestamptz resolved_at
        timestamptz created_at
        timestamptz updated_at
    }

    notifications {
        uuid id PK
        uuid user_id FK
        uuid organization_id FK
        text title
        text message
        notification_type type
        text related_type
        uuid related_id
        boolean is_read
        timestamptz created_at
    }

    activity_logs {
        uuid id PK
        uuid actor_user_id FK
        uuid organization_id FK
        text action
        text related_type
        uuid related_id
        text description
        timestamptz created_at
    }

    auth.users ||--o{ news_releases : created_by
    auth.users ||--o{ transparency_posts : created_by
    auth.users ||--o{ compliance_remarks : created_by
    auth.users ||--o{ compliance_remarks : resolved_by
    auth.users ||--o{ notifications : user_id
    auth.users ||--o{ activity_logs : actor_user_id
    organization_profiles ||--o{ compliance_remarks : organization_id
    organization_profiles ||--o{ notifications : organization_id
    organization_profiles ||--o{ activity_logs : organization_id
```

## Core Tables

- `roles`
- `user_roles`
- `user_profiles`
- `admin_accounts`
- `admin_sessions`
- `policy_versions`
- `user_policy_acceptance`
- `organization_profiles`
- `required_document_types`
- `document_submissions`
- `document_submission_files`
- `budget_requests`
- `budget_request_files`
- `liquidation_reports`
- `liquidation_report_files`
- `news_releases`
- `transparency_posts`
- `compliance_remarks`
- `notifications`
- `activity_logs`
- `news_releases`
- `transparency_posts`
- `compliance_remarks`

## Key Notes

- `PK` means primary key.
- `FK` means foreign key.
- `UK` means unique key.
- Composite unique constraints are used for `user_policy_acceptance (user_id, policy_version_id)` and `document_submission_files (submission_id, document_type_id)`.
- `budget_requests` to `liquidation_reports` is a one-to-one workflow link because each budget request can have at most one liquidation report.
- The subpart figures are intentionally grouped by workflow so the chapter matches the thesis-style layout in your sample.

## Summary

The current schema is limited to the tables that support the current app workflow. Legacy compatibility tables are intentionally not shown in the subpart figures because they are not part of the current chapter scope.
