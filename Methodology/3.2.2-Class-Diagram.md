# 3.2.2 Class Diagram

The class diagram presents the current logical objects used by LYDO Connect.

## Figure 7. Class Diagram of LYDO Connect

```mermaid
classDiagram
    class UserProfile {
        +uuid userId
        +string email
        +string fullName
        +string displayName
        +string contactNumber
        +string municipality
        +string barangay
    }

    class PolicyVersion {
        +uuid id
        +string version
        +string title
        +boolean isActive
        +date effectiveDate
    }

    class UserPolicyAcceptance {
        +uuid id
        +uuid userId
        +uuid policyVersionId
        +boolean acceptedTerms
        +boolean acceptedPrivacy
        +datetime acceptedAt
    }

    class OrganizationProfile {
        +uuid id
        +uuid userId
        +string organizationName
        +string organizationEmail
        +string contactNumber
        +string district
        +string barangay
        +string profileStatus
    }

    class Barangay {
        +uuid id
        +string name
    }

    class RequiredDocumentType {
        +uuid id
        +string name
        +string description
        +string templateUrl
        +boolean isActive
    }

    class DocumentSubmission {
        +uuid id
        +uuid organizationId
        +uuid submittedBy
        +string status
        +boolean userConfirmed
        +datetime submittedAt
    }

    class DocumentSubmissionFile {
        +uuid id
        +uuid submissionId
        +uuid documentTypeId
        +string fileName
        +string fileUrl
        +string validationStatus
    }

    class BudgetRequest {
        +uuid id
        +uuid organizationId
        +uuid submittedBy
        +string activityTitle
        +string activityDescription
        +date activityDate
        +string venue
        +decimal requestedAmount
        +decimal approvedAmount
        +decimal releasedAmount
        +string purposeCategory
        +string status
    }

    class BudgetRequestFile {
        +uuid id
        +uuid budgetRequestId
        +string fileName
        +string fileUrl
    }

    class LiquidationReport {
        +uuid id
        +uuid budgetRequestId
        +uuid organizationId
        +uuid submittedBy
        +string status
        +datetime goSignalAt
        +datetime deadlineAt
    }

    class LiquidationReportFile {
        +uuid id
        +uuid liquidationReportId
        +string fileName
        +string fileUrl
    }

    class NewsRelease {
        +uuid id
        +string title
        +string description
        +string facebookPostUrl
        +date datePosted
        +string visibilityStatus
    }

    class TransparencyPost {
        +uuid id
        +string title
        +string description
        +string category
        +string attachmentUrl
        +string visibilityStatus
    }

    class Notification {
        +uuid id
        +uuid userId
        +uuid organizationId
        +string title
        +string message
        +boolean isRead
    }

    class ActivityLog {
        +uuid id
        +uuid actorUserId
        +uuid organizationId
        +string action
        +string relatedType
        +uuid relatedId
    }

    class ComplianceRemark {
        +uuid id
        +uuid organizationId
        +string relatedType
        +uuid relatedId
        +string message
        +string status
    }

    UserProfile "1" --> "0..*" UserPolicyAcceptance
    PolicyVersion "1" --> "0..*" UserPolicyAcceptance
    UserProfile "1" --> "0..1" OrganizationProfile
    OrganizationProfile "1" --> "0..*" DocumentSubmission
    DocumentSubmission "1" --> "0..*" DocumentSubmissionFile
    RequiredDocumentType "1" --> "0..*" DocumentSubmissionFile
    Barangay "1" --> "0..*" OrganizationProfile
    OrganizationProfile "1" --> "0..*" BudgetRequest
    BudgetRequest "1" --> "0..*" BudgetRequestFile
    BudgetRequest "1" --> "0..1" LiquidationReport
    LiquidationReport "1" --> "0..*" LiquidationReportFile
    OrganizationProfile "1" --> "0..*" Notification
    OrganizationProfile "1" --> "0..*" ActivityLog
    OrganizationProfile "1" --> "0..*" ComplianceRemark
```

## Interpretation

- `UserProfile`, `PolicyVersion`, and `UserPolicyAcceptance` support the authentication and policy gate workflow.
- `OrganizationProfile`, `DocumentSubmission`, `BudgetRequest`, and `LiquidationReport` represent the main organization workflow.
- `Barangay` supports the geographic grouping used for budget allocation monitoring.
- `RequiredDocumentType`, `NewsRelease`, and `TransparencyPost` represent admin-managed reference data and public content.
- `Notification`, `ActivityLog`, and `ComplianceRemark` support workflow updates and traceability.
