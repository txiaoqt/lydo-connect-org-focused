# 3.2.2 Class Diagram

The class diagram presents the current logical objects used by LYDO Connect.

## Figure 7. Class Diagram of LYDO Connect

```mermaid
classDiagram
    class OrganizationProfile {
        +uuid id
        +uuid userId
        +string organizationName
        +string organizationEmail
        +string contactNumber
        +string barangay
        +string profileStatus
    }

    class DocumentSubmission {
        +uuid id
        +uuid organizationId
        +uuid submittedBy
        +string status
        +boolean userConfirmed
        +datetime submittedAt
    }

    class SubmissionFile {
        +uuid id
        +uuid submissionId
        +uuid documentTypeId
        +string fileName
        +string fileUrl
        +string validationStatus
        +string adminStatus
    }

    class BudgetRequest {
        +uuid id
        +uuid organizationId
        +uuid submittedBy
        +string activityTitle
        +string activityDescription
        +date activityDate
        +string venue
        +decimal requestedAmount
        +string purposeCategory
        +string status
    }

    class BudgetRequestFile {
        +uuid id
        +uuid budgetRequestId
        +string fileName
        +string fileUrl
    }

    class LiquidationReport {
        +uuid id
        +uuid budgetRequestId
        +uuid organizationId
        +uuid submittedBy
        +string status
        +datetime goSignalAt
        +datetime deadlineAt
    }

    class LiquidationReportFile {
        +uuid id
        +uuid liquidationReportId
        +string fileName
        +string fileUrl
    }

    class NewsRelease {
        +uuid id
        +string title
        +string description
        +string facebookPostUrl
        +date datePosted
        +string visibilityStatus
    }

    class TransparencyPost {
        +uuid id
        +string title
        +string description
        +string category
        +string attachmentUrl
        +string visibilityStatus
    }

    class TemplateRecord {
        +uuid databaseId
        +string name
        +string description
        +string templateUrl
        +boolean isActive
    }

    class NotificationRecord {
        +uuid id
        +uuid userId
        +uuid organizationId
        +string title
        +string message
        +boolean isRead
    }

    class ActivityLog {
        +uuid id
        +uuid actorUserId
        +uuid organizationId
        +string action
        +string description
    }

    class ComplianceRemark {
        +uuid id
        +uuid organizationId
        +string relatedType
        +string relatedId
        +string message
        +string status
    }

    OrganizationProfile "1" --> "0..*" DocumentSubmission
    DocumentSubmission "1" --> "0..*" SubmissionFile
    OrganizationProfile "1" --> "0..*" BudgetRequest
    BudgetRequest "1" --> "0..*" BudgetRequestFile
    BudgetRequest "1" --> "0..1" LiquidationReport
    LiquidationReport "1" --> "0..*" LiquidationReportFile
    OrganizationProfile "1" --> "0..*" NotificationRecord
    OrganizationProfile "1" --> "0..*" ActivityLog
    OrganizationProfile "1" --> "0..*" ComplianceRemark
```

## Interpretation

- `OrganizationProfile`, `DocumentSubmission`, `BudgetRequest`, and `LiquidationReport` represent the main organization workflow.
- `NewsRelease`, `TransparencyPost`, and `TemplateRecord` represent admin-managed content and reference data.
- `NotificationRecord`, `ActivityLog`, and `ComplianceRemark` support workflow updates and traceability.
